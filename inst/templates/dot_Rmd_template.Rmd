---
params:
  project_title: ""
  project_short_title: ""
  redmine_issue: "#"
  requester_name: ""
  requester_mail: ""
  pi_name: ""
  pi_mail: ""
  organisation: ""
  staff_name: ""
  staff_mail: ""
  staff_web: ""
title: "`r params$project_title`"
date: "`r format(Sys.time(), '%d %B, %Y')`"
output:
  html_document:
    toc: true
    toc_float: 
      collapse : false
    toc_depth: 4
    # code_folding: hide
    number_sections: true
knit: (function(inputFile, encoding) {
  rmarkdown::render(inputFile, encoding= encoding, output_dir= "../reports") })
---

<style>
p.caption {
  font-size: 0.8em;
  font-family: Helvetica, sans-serif;
}
</style>


```{r Setup, echo= FALSE, include=FALSE}
knitr::opts_chunk$set(
	echo= F,    # comment out for code_folding
	# dev= c('png', 'pdf'),   # save figures in two formats
	# fig.path= '../reports/figure/',  # save figure in files
  fig.width = 8,
  fig.height= 6,
  results   = "asis",
  message   = FALSE,
  warning   = FALSE
)
```

```{r loading}

###  Packages

library(tidyverse)
library(NBISsupport)    # english, english_cap1, enum_vars
  # renv::install("Rundmus/NBISsupport-R_package")
library(knitr)
# library(kableExtra)     # kbl, kable_styling
# library(janitor)        # tabyl, adorn_*

###  Options
options(
  #  NA in `kable` is displayed as ''
  knitr.kable.NA = '',
  #  prevent "`summarise()` ungrouping output (override with `.groups` argument)"
  dplyr.summarise.inform= FALSE
)

###  Clean up the workspace
rm(list= ls()[ls() != 'params'])

###  File names

fn <- list(
	i = list(                               #  input
	)
)

#  Check all exists
stopifnot(all(file.exists(c('.', unlist(fn$i)))))

###  CONSTANTS

```

#  Project information

 * Redmine issue: **`r params$redmine_issue`**
 * NBIS staff: **`r params$staff_name`**
 * Request by: **`r params$requester_name`**
 * Principal investigator: **`r params$pi_name`**
 * Organisation: **`r params$organisation`**

----------------------------

#  Data

```{r load}

```


##  Dimension

* Number of samples : 
* Number of 

----------------------------

#  Analysis



----------------------------

#  Appendix

##  R environment

```{r sessionInfo, results='markup'}
sessionInfo()
```


